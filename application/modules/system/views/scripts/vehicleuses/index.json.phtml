
<?php

$output = array(
    "sEcho" => intval($_GET['sEcho']),
    "iTotalRecords" => $this->items->getTotal(),
    "iTotalDisplayRecords" => $this->items->getFilteredTotal(),
    "aaData" => array(),
);

foreach ($this->items->fetchAll() as $item) {
    $row = array();
    $row[] = '<input type="checkbox" name="itemid[]" class="check-item" value="' . $item->vehicle_use_id . '"/>';
    $row[] = $item->vehicle_use;
    $row[] = '<a href="/system/vehicletypes/edit/id/' . $item->vehicle_use_id . '">Edit</a>&nbsp;&nbsp;<a class="removeItem" href="/system/vehicletypes/delete/id/' . $item->vehicle_use_id . '">Delete</a>&nbsp;&nbsp;';
    $output['aaData'][] = $row;
}
echo json_encode($output);
?>